# Core API framework (versions compatible with TensorFlow 2.13)
fastapi==0.100.0
uvicorn[standard]==0.23.0
python-multipart==0.0.6

# ML/Audio processing
# TensorFlow CPU build (smaller image, faster startup on Render)
tensorflow-cpu==2.13.*
xgboost==2.0.3
librosa==0.10.1
numpy==1.24.3
soundfile==0.12.1

# Utilities (compatible with TF 2.13's typing-extensions<4.6.0)
pydantic==1.10.12
